import React from "react";
import Card from "./Card";
import "./Home.css";

const Home = (props) => {
  return (
    <Card className="home">
      <h1>Welcome back!</h1>
    </Card>
  );
};

export default Home;
